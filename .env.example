# Base de datos
DB_HOST=localhost
DB_PORT=5432
DB_NAME=findutabs
DB_USERNAME=findutabs
DB_PASSWORD=change_me_in_production

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT - MUST be at least 512 bits (64 characters) for HS512
JWT_SECRET=change_this_to_a_random_512_bit_secret_in_production_never_commit_real_secrets
JWT_EXPIRATION_MS=86400000

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Spring
SPRING_PROFILES_ACTIVE=dev

# AI Service
AI_SERVICE_URL=http://localhost:8000
